# 本地验收记录

## 2026-09-15 陪陪管理

- 新增名单页面与完整CRUD接口，旧样本自动关联9位陪陪。
- 数据库测试覆盖同名冲突、备注搜索、旧订单迁移、新订单选择、改名与删除不改变历史订单、删除后拒绝新订单引用、重复导入不恢复删除项。
- 浏览器实际新增“陪陪UI验收”、改名、确认录单下拉可选择、最后删除临时资料；保留9位原陪陪与24条原订单。
- 手机导航与直接可见的编辑／删除按钮已检查。

2026-09-14 完成。

- 24 条 September 样本与 Excel 逐条核对：总金额 RM665.00、陪陪工资 RM500.45、剩下 RM164.55。
- 7 组计算／校验测试通过，包括服务别名、手动单价、分成优先级、小数数量、独立礼物和编号扩展。
- MongoDB 集成测试通过：20 个并发订单不重复、三类独立编号、删除不复用、类型不可更改、分页、组合筛选、汇总和重连。
- 重复导入不会新增重复记录，也不会覆盖编辑或恢复已删除的样本。
- 浏览器实际新增独立礼物 L0001，RM100 独家礼物显示工资 RM85；编辑为“已付款”，刷新和数据库重启后保持一致；最后通过网页删除确认清理该验收订单。
- 因为编号不回收，本机下一笔礼物单从 L0002 开始。全新数据库仍从 L0001 开始。
- 数据库关闭时 API 返回503，网页显示连接失败与重试按钮；重启后恢复。
- 检查桌面与390px手机页面、手机录单表单、弹窗 Tab 焦点循环。手机订单表可横向滚动查看费用和操作。
- `start-local.ps1` 实际完成依赖安装、MongoDB 启动、幂等样本导入与网页启动；停止数据库脚本已验证正常关闭并保留数据。
- 类型检查、测试和 Next.js 生产构建通过。

交付包含源码、依赖锁文件、样本数据、启动脚本、环境变量示例和 GitHub Actions。GitHub／Vercel 连接和线上部署尚未执行，按第一部分范围保留到后续。
